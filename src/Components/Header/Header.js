import Container from '../Container/Container';

const header = (props) => {
    return(
        <Container>
            <header>Hello world!</header>
        </Container>
    )
};

export default header;