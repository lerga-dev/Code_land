import Container from '../Container/Container';

const column = (props) => {
    return(
        <Container>
            <div>This is a column!</div>
        </Container>
    )
};

export default column;